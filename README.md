# DocTruth

Runs library documentation snippets against the actually-installed version and publishes verified "works / breaks + real output" pages. The result is executed, not guessed. Built + deployed by GitHub Actions (see .github/workflows/build.yml).
